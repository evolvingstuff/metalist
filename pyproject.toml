[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "notes-app"
version = "0.1.0"
description = "A secure note-taking application with server-side rendering"
requires-python = ">=3.8"
license = {text = "MIT"}