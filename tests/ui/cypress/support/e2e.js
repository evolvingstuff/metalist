// Import commands.js using ES2015 syntax:
import './commands'

// Note: The server should be started with --test flag or TEST_MODE=1 environment variable
// This will automatically use test.db instead of notes.db and clear it on startup
// Example: TEST_MODE=1 uvicorn app.main:app --reload
//      or: python -m uvicorn app.main:app --test 