## 2024.12.26

12:40am:

So the undo/redo is the issue for now. Unless there is something really 
strange with the UI, it is clearly broken.

I think I'm going to make up some smaller test cases, and start with only 
certain kinds of operations. The easiest I think is the undo of the 
note content update.

I might also extract out some of the commonly used functions in the tests.

----

1:51am

I've got the undo/redo running in a test.
Needed to wrap the ops in decorators using lambdas so the actions would 
be captured on the command stack.

Currently just testing the update_note function. Undo seems to work fine,
and redo is failing.

----

2:41am

Interesting - looking at the command stack, it has the correct pre values, 
but does not pick up on the correct post values:

```
Command stack: [Command:
        pre_state:
                0 content=""
        post_state:
                0 content=""

```

----

3:51am

Fixed it! I got the post-state data captured correctly. I had to use a 
hacky locking mechanism to make sure it didn't go into an infinite loop,
but the simple test case is working now.

----

5:00am

Confirmed UI is working for Undo and Redo for the update_note function.

Still totally broken for moving items or adding/deleting. However, I realized
this is a bit strange because technically the moving of notes is just updates 
to attributes of `prev_id`, `next_id`, and `parent_id`. So my default intuition 
is that if `content` is working, then the others should work as well.

----

7:00am

UNDO/REDO IS WORKING COMPLETELY!!!

----

9:05am

Found a bug: unable to add children or siblings to a note.

Functions that don't work:
* "/new-drop"
* "/new-sibling/{note_id}"
* "/new-child/{note_id}"

Hmmm... just created some tests, and all of those functions (on the server)
pass, so it isn't the operations. I'm guessing it is something in the 
decorators in the API that is causing the issue.

----

## Next Possible Steps...
* fix bug not adding children / siblings