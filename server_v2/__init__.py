"""Server V2 (Phase 0): View-only API mounted at /api2.

Contains a read-only in-memory store hydrated at startup and a simple
view builder to return structure + notes from memory with zero DB reads
after startup.
"""

